machine:
  ruby:
    version:
      2.0.0

dependencies:
  override:
    - bundle install:
        timeout: 180

test:
  post:
    - bundle exec rake test

deployment:
  production:
    branch: master
    heroku:
      appname: passwdgen

